
const initialState={
  project:[],
  loading:false,
  error:null,
projectDetails:null,
  searchedProject: [],
};

export const authReducer=(state=initialState,action)=>{
  switch(action.type)
  {
    
    default : 
    state;
  }
}