
const initialState={
  user :null,
  loading:false,
  error:null,
  jwt : null,
  projetcSize : 0,
}

export const authReducer=(state=initialState,action)=>{
  switch(action.type)
  {
    
    default : 
    state;
  }
}